#include 'main.h'

/**
 *main - Entry point
 * Return: Always 0.
 */
void print_alphabet(void)
{
	int abc;
	/*this will print the full alphabet with a new line at the end*/
	for (abc = 'a'; abd <= 'z'; abc++)
		_putchar(abc);
	_putchar('\n');

